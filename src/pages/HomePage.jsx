/**
 * Landing / Home page — Phase 0 placeholder. Will be replaced with the full
 * landing page (hero, featured courses, testimonials) in a later phase.
 */
export function HomePage() {
  return (
    <section className="mx-auto max-w-page px-4 py-20 text-center">
      <h1 className="text-display font-bold text-carbon">
        Learn without limits
      </h1>
      <p className="mx-auto mt-4 max-w-text text-body text-ash">
        Start, switch, or advance your career with thousands of courses from
        expert instructors.
      </p>
    </section>
  );
}
