/**
 * Student Dashboard — Phase 1 placeholder. Will be replaced with the full
 * dashboard (enrolled courses, stats, recent activity) when the student
 * feature is built.
 */
export function DashboardPage() {
  return (
    <section className="px-6 py-10">
      <h1 className="text-heading font-semibold text-carbon">Dashboard</h1>
      <p className="mt-2 text-body text-ash">
        Student dashboard — coming soon.
      </p>
    </section>
  );
}
